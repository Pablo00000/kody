/*
 * plik.cxx

 */

#include <iostream>
using namespace std;

int main(int argc, char **argv)
{
	float sw;
	cout << "Podaj sw" << endl;
	cin >> sw;
	float p;
	cout << "Podaj p" << endl;
	cin >> p;
	float so;
	so = (sw * p)/100;s
	cout << "so wynosi: " << so << endl;
	
	return 0;
}

